<?xml version="1.0" encoding="UTF-8"?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms" 
              xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup" 
              xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="APP_KTRA_ROUTER.Popup.Cbox_Tram">
    <pages:PopupPage.Animation>
        <animations:MoveAnimation 
            PositionIn="Top" 
            PositionOut="Bottom">
        </animations:MoveAnimation>
    </pages:PopupPage.Animation>
    <Frame CornerRadius="15"
           OutlineColor="Black"    
          
           HorizontalOptions="FillAndExpand"
           VerticalOptions="StartAndExpand">
        
        <StackLayout HorizontalOptions="FillAndExpand">
            <Grid ColumnDefinitions="9*,1*" RowDefinitions="30">   
                <SearchBar Grid.Column="0" HorizontalOptions="FillAndExpand" 
                       x:Name="search" 
                       Placeholder="Chọn trạm cần tìm"
                       HeightRequest="30"
                       TextChanged="search_TextChanged"
                       ></SearchBar>
                <Image Grid.Column="1"  x:Name="imgClose" Source="close.png" Aspect="AspectFit" HorizontalOptions="End" HeightRequest="30" WidthRequest="30">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped">

                        </TapGestureRecognizer>
                    </Image.GestureRecognizers>

                </Image>
            </Grid>
            
            <ScrollView Orientation="Horizontal">
                <StackLayout>
                    <Grid ColumnDefinitions="100,200,200" RowDefinitions="30" HorizontalOptions="FillAndExpand">
                        <Label Text="MÃ TRẠM" Grid.Column="0"  FontSize="14" FontAttributes="Bold" HorizontalTextAlignment="Center" ></Label>
                        <Label Text="TÊN TRẠM" Grid.Column="1" FontSize="14" FontAttributes="Bold" HorizontalTextAlignment="Center"></Label>
                        <Label Text="ĐỊA CHỈ" Grid.Column="2" FontSize="14" FontAttributes="Bold" HorizontalTextAlignment="Center"></Label>
                    </Grid>
                    <ListView x:Name="listViewTram"
                      ItemsSource="{Binding _ListTram}"
                      HasUnevenRows="True"
                      HorizontalOptions="FillAndExpand"       
                      VerticalOptions="FillAndExpand"
                      ItemSelected="listTram_ItemSelected"
                      CachingStrategy="RecycleElement">

                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <ViewCell.View >
                                        <Grid ColumnDefinitions="100,200,200" HorizontalOptions="FillAndExpand" RowSpacing="5" Padding="0,5,0,5">
                                            <Label Text="{Binding MA_TRAM}" Grid.Column="0" TextColor="#149C62" FontSize="18" HorizontalTextAlignment="Start"></Label>
                                            <Label Text="{Binding TEN_TRAM}" Grid.Column="1"  TextColor="#149C62" FontSize="18" HorizontalTextAlignment="Start"></Label>
                                            <Label Text="{Binding DIA_CHI}" Grid.Column="2"  TextColor="#149C62" FontSize="18" HorizontalTextAlignment="Start">
                                            </Label>
                                        </Grid>
                                    </ViewCell.View>
                                </ViewCell>

                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackLayout>                
            </ScrollView>          

        </StackLayout>
    </Frame>
</pages:PopupPage>